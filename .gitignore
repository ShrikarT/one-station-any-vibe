node_modules/

# Generated by `npm run audio` (postinstall). The manifest is committed, the
# audio itself is not: 12MB of wav in git history helps nobody, and anyone who
# runs `npm install` gets identical files from the same seeds.
public/audio/*.wav

.DS_Store
*.log
.env*
